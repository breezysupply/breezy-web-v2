# Development settings (production values are set in Amplify Console)
NODE_ENV=development
PORT=8080
HOST=0.0.0.0

# S3 access is handled by IAM role in production
# No credentials needed locally if using AWS CLI default profile

